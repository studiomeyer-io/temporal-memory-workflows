# Local Temporal cluster (this repo's infrastructure/dev2/docker-compose.yml)
TEMPORAL_ADDRESS=127.0.0.1:7233
TEMPORAL_NAMESPACE=memory-workflows

# StudioMeyer Memory — hosted SaaS (or compatible REST endpoint)
NEX_MEMORY_URL=https://memory.studiomeyer.io
NEX_MEMORY_API_KEY=sk_replace_me

# Optional: LLM for T01's reasoning activity (mocked in tests)
ANTHROPIC_API_KEY=sk-ant-replace_me
